# Skill Registry — ingenieria_software

Generated by `sdd-init`. Index only — sub-agents read the exact `SKILL.md` paths listed below, not this summary.

## Project Skills

None found. No `skills/`, `.claude/skills/`, `.opencode/skills/`, `.cursor/skills/`, `.github/skills/`, `.codex/skills/`, `.qwen/skills/`, `.kiro/skills/`, `.openclaw/skills/`, `.pi/skills/`, `.agent/skills/`, `.agents/skills/`, or `.atl/skills/` directories exist in this repository.

## User-Level Skills (deduplicated across `~/.claude/skills`, `~/.config/opencode/skills`, `~/.cursor/skills`, `~/.copilot/skills` — identical sets)

SDD phase skills (`sdd-*`) and `_shared`/`skill-registry` are omitted per scan rules — the orchestrator/executors already know these paths.

| Skill | Trigger (from description) | Path |
|---|---|---|
| branch-pr | Create Gentle AI pull requests with issue-first checks. PR creation/opening/review prep. | `~/.claude/skills/branch-pr/SKILL.md` |
| chained-pr | PRs over 400 lines, stacked PRs, review slices — split oversized changes. | `~/.claude/skills/chained-pr/SKILL.md` |
| cognitive-doc-design | Design docs that reduce cognitive load — guides, READMEs, RFCs, onboarding, architecture docs. | `~/.claude/skills/cognitive-doc-design/SKILL.md` |
| comment-writer | Write warm, direct collaboration comments — PR feedback, issue replies, reviews, Slack/GitHub comments. | `~/.claude/skills/comment-writer/SKILL.md` |
| go-testing | Go tests, go test coverage, Bubbletea teatest, golden files. (Not applicable to this C#/.NET repo — no Go code.) | `~/.claude/skills/go-testing/SKILL.md` |
| issue-creation | Create Gentle AI issues with issue-first checks — bug reports, feature requests. | `~/.claude/skills/issue-creation/SKILL.md` |
| judgment-day | Blind dual review, adversarial review — run confirmed-issue fixes, then re-judge. | `~/.claude/skills/judgment-day/SKILL.md` |
| skill-creator | Create LLM-first skills with valid frontmatter. | `~/.claude/skills/skill-creator/SKILL.md` |
| skill-improver | Audit and upgrade existing LLM-first skills. | `~/.claude/skills/skill-improver/SKILL.md` |
| work-unit-commits | Plan commits as reviewable work units — implementation, commit splitting, chained PRs. | `~/.claude/skills/work-unit-commits/SKILL.md` |

## Project Convention Files Scanned

- `CLAUDE.md` (project root) — FOUND. Extensive: build commands (MSBuild, no dotnet CLI), architecture (CAPAS/BLL/DAL/BE/SeguridadYServicios), ReaLTaiizor UI conventions, integrity digest (DVH/DVV) rules, permission model (Composite pattern, no inheritance), Observer pattern for i18n, Singleton (SessionManager/IdiomaManager), USUARIO_HISTORIAL audit rules, DB conventions (stored procedures only), diagram/doc generation workflow, use-case modeling conventions (2 actors only: Usuario/Administrador).
- `README.md` (project root) — FOUND, not re-read in full for this init (CLAUDE.md is the authoritative agent-facing doc).
- `AGENTS.md`, `agents.md`, `.cursorrules`, `GEMINI.md`, `.github/copilot-instructions.md` — NOT FOUND.

## Notes for Downstream SDD Phases

- No project-specific skill exists for C#/WinForms/ADO.NET/stored-procedure conventions — the project's own `CLAUDE.md` is the closest equivalent and should be treated as required reading for `sdd-explore`/`sdd-propose`/`sdd-design`/`sdd-apply` on this repo.
- `chained-pr` and `work-unit-commits` are relevant if a change here grows past the 400-line review budget.
- `go-testing` is irrelevant to this stack; kept in the index only because it exists at user level.
